# See .env.local.example (local/offline Docker deployment) or
# .env.production.example (cloud deployment) — copy whichever matches your
# deployment to .env. This file is kept only so a bare `cp .env.example .env`
# still works and points you to the right one.
